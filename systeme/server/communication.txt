__________________SYNTAXE DES REQUÊTES________________________________

---------------------
		Menu
---------------------
	Open: 
		Client : "_pid_t process_client" via le pipe connexion
	Connexion:
		Client : "Ma_pseudo_mdp" via pipe pid_t
		Server : "true"/"false" via le pipe pid_t 

	Inscription: 
		Client : "Mb_pseudo_mdp" via pipe pid_t
		Server : "true"/"false" via le pipe pid_t 

	addfriend:
		Client : "Mc_PseudoMe_PseudoF" via pipe pid_t
		Server: "0=sent"

	delfriend:
		Client : "Md_Pseudo" via pipe pid_t                     // plutot "Md_PseudoMe_PseudoF"
		Server: "True"/ "False" 

	CheckleaderBoard: 
		Client : "Me" via pipe pid_t
		Server: "pseudo1&score|pseudo2&score|pseudo3&score" via pipe pid_t

	Liste_d'amis:
		Client : "Mf_pseudo" via pipe pid_t
		Server: "tableau de mes amis avec pseudo | score" 

	sendFriendRequest:
		Client : "Mc_PseudoMe_PseudoF" via pipe pid_t
		Server: "2=erreur"/ "1=already" / "0=sent"

	getFriendRequests:
		Client : "Mg_Pseudo" via pipe pid_t
		Server: "pseudo1_pseudo2_pseudo3_pseudo4..." 

	View profile
		Client : "Mh" via pipe pid_t
		Server: "Pseudo_Score" via pipe pid_t
	Log_out:
		Client : "Mi" via pipe pid_t
		Server: "Score" via pipe pid_t




----------------------
	CREE UNE PARTIE
----------------------
	Verification identité second joueur :
		même chose que dans connexion ! (pose pas de question)

	Cree une partie:
		Client : "P_pseudoHote_pseudoj2_" via pipe pid_t //pseudo2 = Null 	si personne
		Server : "ID" via le pipe pid_t 

	Action:
		Client : "J_Action_ID" via pipe pid_t
		Server : "IMAGE du jeux" via le pipe pid_t 






